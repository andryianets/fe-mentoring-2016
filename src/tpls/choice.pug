- id = [name, value].join('_');

label(for=id,
class=(checked && 'selected'))
    input(
    type="radio",
    id=id
    name=name, value=value,
    onchange="appMediator.onChoiceSelected(this)",
    checked=checked)
    | #{label}